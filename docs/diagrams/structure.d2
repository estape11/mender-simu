direction: right

mender-simulator: {
  src: {
    mender_simulator: {
      db: {
        models.py: "Device, DeploymentStatus"
        database.py: "DatabaseManager (async)"
      }
      client: {
        auth.py: "RSA signing, JWT"
        inventory.py: "PATCH attributes"
        deployments.py: "Polling & status"
        exceptions.py: "AuthenticationError"
      }
      simulation: {
        profiles.py: "Industry generators"
        device_simulator.py: "Update lifecycle"
      }
      utils: {
        config.py: "YAML loader"
        crypto.py: "RSA keypairs"
      }
      main.py: "FleetOrchestrator"
    }
  }
  scripts: {
    create-demo-artifacts.sh
    cleanup-devices.sh
  }
  tests: {
    test_*.py
  }
  config: {
    config.yaml
  }
}
